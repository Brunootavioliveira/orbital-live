spring:
  application:
    name: solar-system-nasa

  data:
    redis:
      host: localhost
      port: 6379

nasa:
  horizons:
    url: https://ssd.jpl.nasa.gov/api/horizons.api

  scheduler:
    interval: 30000

server:
  port: 8080
