spring:
  application:
    name: solar-system-nasa
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

nasa:
  horizons:
    url: ${NASA_HORIZONS_URL}
  scheduler:
    interval: ${NASA_SCHEDULER_INTERVAL:30000}

server:
  port: ${SERVER_PORT:8080} 